<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
                 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mycompany.webapp.dao.mybatis.BoardDao">
	<select id="count" resultType="int">
		SELECT COUNT(*) FROM boards
	</select>
	
	<select id="countByUserid" parameterType="string" resultType="int">
		SELECT COUNT(*)
		FROM boards
		WHERE boardwriter=#{userid}
	</select>
	
	<select id="selectByBoardno" parameterType="int" resultType="board">
		SELECT boardno, boardwriter, boardtitle,
			boardcontent, boarddate, bimagecontenttype,
			bimageoriginalfilename, bimagesavedfilename
		FROM boards
		WHERE boardno=#{boardno}
	</select>
	
	<select id="selectByPage" parameterType="pager" resultType="board">
		SELECT rnum, boardno, boardtitle, boarddate, boardwriter
		FROM (
		  SELECT ROWNUM as rnum, boardno, boardtitle, boarddate, boardwriter
		  FROM (
		    SELECT boardno, boardtitle, boarddate, boardwriter 
		    FROM boards
		    ORDER BY boardno DESC
		  )
		  WHERE rownum &lt;= #{endRowNo}
		)
		WHERE rnum &gt;= #{startRowNo}
	</select>
	
	<select id="selectByUserid" resultType="board">
		SELECT rnum, boardno, boardtitle, boarddate, boardwriter
		FROM (
			SELECT ROWNUM as rnum, boardno, boardtitle, boarddate, boardwriter
			FROM (
				SELECT boardno, boardtitle, boarddate, boardwriter
				FROM boards
				WHERE boardwriter=#{userid}
				ORDER BY boardno DESC
			)
			WHERE rownum &lt;= #{pager.endRowNo}
		)
		WHERE rnum &gt;= #{pager.startRowNo}
	</select>
	
	<insert id="insert" parameterType="board">
		INSERT INTO boards (boardno, boardwriter, boardtitle,
							boardcontent, boarddate, bimagecontenttype,
							bimageoriginalfilename, bimagesavedfilename)
		VALUES(#{boardno}, #{boardwriter}, #{boardtitle},
				#{boardcontent}, SYSDATE, #{bimagecontenttype},
				#{bimageoriginalfilename}, #{bimagesavedfilename})
	</insert>
	
	<update id="update" parameterType="board">
		UPDATE boards
		SET btitle=#{btitle}, bcontent=#{bcontent}
		WHERE bno=#{boradno}
	</update>
	
	<delete id="deleteByBoardno" parameterType="int">
		DELETE FROM boards
		WHERE bno=#{boradno}
	</delete>
	<!-- 
	<insert id="insert" parameterType="board">
		<selectKey keyProperty="bno" resultType="int" order="BEFORE">
			select SEQ_BNO.nextval from dual
		</selectKey>
		<if test="battachoname == null">
			INSERT INTO board (bno, btitle, bcontent, bdate, mid, bhitcount, battachoname, battachsname, battachtype)
			VALUES(#{bno}, #{btitle}, #{bcontent}, SYSDATE, #{mid}, 0, null, null, null)
		</if>
		<if test="battachoname != null">
			INSERT INTO board (bno, btitle, bcontent, bdate, mid, bhitcount, battachoname, battachsname, battachtype)
			VALUES(#{bno}, #{btitle}, #{bcontent}, SYSDATE, #{mid}, 0, #{battachoname}, #{battachsname}, #{battachtype})
		</if>
	</insert>
	
	<delete id="deleteByBno" parameterType="int">
		DELETE FROM board WHERE bno=#{bno}
	</delete>
	
	<update id="update" parameterType="board">
		UPDATE board SET btitle=#{btitle}, bcontent=#{bcontent} WHERE bno=#{bno}
	</update> -->
</mapper>


